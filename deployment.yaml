apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-python-api
spec:
  selector:
    matchLabels:
      app: simple-python-api
  template:
    metadata:
      labels:
        app: simple-python-api
    spec:
      containers:
      - name: simple-python-api
        image: diegodejesus/simple-python-api:f2e68de500bb1157f4e1ffa384808a16269d8d00
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: simple-python-api
spec:
  selector:
    app: simple-python-api
  ports:
  - port: 8000
    targetPort: 8000

